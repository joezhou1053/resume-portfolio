1. .shape

arr.shape 返回一个元组，描述数组的维度。示例：

a = np.array([1,2,3])
a.shape  # -> (3,) —— 一维，3 个元素


若是二维数组（矩阵）：(rows, cols)，比如 (500,1) 表示 500 行 1 列。


2. shape
2. 比喻讲解

你可以把 DataFrame 想成一个 Excel 表格。

.shape[0] 就是 有多少行（多少条记录）。

.shape[1] 就是 有多少列（多少个字段）。

👉 所以 .shape = (行数, 列数)
👉 .shape[0] = 行数
👉 .shape[1] = 列数


3. 关联系数
np.corrcoef(x ,y)


4. np.meshgrid
是 NumPy 里超常用的函数，尤其在机器学习、画等高线图、可视化决策边界的时候。

numpy.meshgrid(*xi, copy=True, sparse=False, indexing='xy')
输入的一维坐标向量，可以传两个（常见 2D 网格），也可以传三个（3D 网格），甚至更多维度。
举例:
x = np.linspace(-1, 1, 3)   # [-1, 0, 1]
y = np.linspace(-2, 2, 5)   # [-2, -1, 0, 1, 2]
X, Y = np.meshgrid(x, y)

输出: 
返回多个数组，每个数组代表一个坐标轴上的坐标点展开成网格后的结果。

5. np.ravel()
它是 NumPy 里一个超常用的数组方法，尤其在数据展开、可视化、机器学习特征处理时特别方便。

ndarray.ravel(order='C')

.ravel() 的核心就是：
把多维数组拉平成一维数组。


6. .c_

.c_ 在 NumPy 里特别有意思，常用来拼接数组，写法超级简洁。咱们来逐步拆解

np.c_[obj1, obj2, ...]


.c_ 本质上是 一个快捷索引器，主要功能是：

把一维数组按列拼接成二维数组。

把多个数组横向拼接（列方向 concat）。

示例: 把 1D 变 2D 列向量

7. reshape 

numpy.reshape(a, newshape, order='C')
 scaler.fit(df.Close.values.reshape(-1,1)) 

arr = np.arange(6)   # [0, 1, 2, 3, 4, 5]

# 变成 2 行 3 列
print(arr.reshape(2, 3))
# [[0 1 2]
#  [3 4 5]]

-1 = 自动补全，很省心。
# 用 -1 自动推断（系统算出剩下的维度）
print(arr.reshape(-1, 2))
# [[0 1]
#  [2 3]
#  [4 5]]

例子3: 
X_train = np.reshape(X_train, (X_train.shape[0], X_train.shape[1],1))
a= X_train
newshape = (X_train.shape[0], X_train.shape[1],1)
(A, B, C)
A= X_train.shape[0]
B= X_train.shape[1]
C =1
三维的

8 .np.array
np.array(annual_return)

9. .T转置